import type { IPluginMeta } from "@/types";

export const meta: IPluginMeta = {
  id: "emissions-table",
  displayName: "Tabela Customizada",
  description: "Renderiza uma tabela com dados filtrados.",
  version: "1.0.0",
  tags: ["tabela", "dashboard"],
};
